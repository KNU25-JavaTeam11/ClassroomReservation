<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Spring 백엔드 연동 관련 로그는 DEBUG 레벨로 -->
    <logger name="org.javateam11.ClassroomReservation.service" level="DEBUG"/>
    
    <!-- 루트 로거는 INFO 레벨로 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration> 